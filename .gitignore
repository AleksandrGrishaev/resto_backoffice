# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables (NEVER commit secrets!)
.env
.env.local
.env.*.local
.env.development
.env.production
.env.staging
.env.seed.production  # CRITICAL: Contains SERVICE KEY for seeding
.env.backup           # Database password for backups
.env.restore.dev      # DEV database password for restore

# Backups (contain sensitive data!)
backups/

# Production credentials (NEVER commit!)
PRODUCTION_CREDENTIALS.md

# Keep example for documentation
!.env.example

# Vercel
.vercel
*.vercel.json

# Playwright MCP temporary files
.playwright-mcp/

# MCP config (contains environment-specific database refs)
.mcp.json

# Temporary data files
Temp/
