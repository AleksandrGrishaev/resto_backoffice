# Supabase Service Key Template
# Copy this to .env.development and .env.production

# ===== HOW TO GET SERVICE KEY =====
# 1. Go to: https://supabase.com/dashboard/project/fjkfckjpnbcyuknsnchy/settings/api
# 2. Find section: "Project API keys"
# 3. Copy the "service_role" key (NOT "anon" key!)
# 4. Paste below

# ===== IMPORTANT SECURITY WARNING =====
# ⚠️ Service key BYPASSES Row Level Security (RLS) policies
# ⚠️ Use ONLY in development/testing with PIN auth
# ⚠️ NEVER commit service key to git
# ⚠️ NEVER use in production with public access

# Add these lines to your .env.development:
VITE_SUPABASE_SERVICE_KEY=your-service-role-key-here
VITE_SUPABASE_USE_SERVICE_KEY=true

# Add these lines to your .env.production (when using Supabase Auth):
VITE_SUPABASE_SERVICE_KEY=your-service-role-key-here
VITE_SUPABASE_USE_SERVICE_KEY=false  # Use anon key + RLS in production
